// Archivo JS para registrar personal
// Se reserva para futuras funciones específicas del formulario

// Enfocar automáticamente el primer campo al cargar la página
window.addEventListener('DOMContentLoaded', () => {
  const firstInput = document.querySelector('.form-grid input');
  if (firstInput) {
    firstInput.focus();
  }
});
